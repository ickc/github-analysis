[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "github-analysis"
version = "0.1.0"
description = "Reusable tools for GitHub analysis"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/github_analysis"]
